现有版本已经较好的支持了按home模式进行展示。现在考虑到任何一个有权限的人都能很方便的看到所有的home星系，不利于信息的隐蔽（虽然都具备权限，但是为了避免eve online中的经典spy玩法（现有模式他只要进来就能获取到我们的所有home星系以及入口）。所以准备进行如下更新：
1. 对于非admin和manager用户，移除默认的全局展示（all视图）
2. 强制用户在第一次进入地图时，需要设置自己的home星系（也或者说关注/订阅星系），可以设置多个星系。根据acl中的权限，admin和manager可以订阅的星系不做数量限制，member最多可以订阅3个星系，viewer只能订阅1个星系。
3. 根据订阅的数量，现有的home视图机制需要调整，也就是说不再只针对单个星系，而是多个星系在同一个视图里作为home（关注/订阅的星系始终会在画布上展示），并且即使没有设置home状态，也可以使用排序功能。
4. 移除现有的all和home的筛选切换，用户可以修改订阅的星系然后重新加载视图，但是看不到别人的订阅，也无法看到所有的home星系列表。
5. 菜单里的搜索功能，列表中不再加载全局地图的星系，而是跟随用户选择了订阅的星系之后的当前视图（也就是当前地图上看不到的，列表上也看不到）

换句话说，管理员仍然可以看到和使用全局视图，但是普通成员只能使用订阅星系（1个或者多个）的方式看到和订阅星系相连的星系视图和链接。


1. 订阅的问题，第一次进入地图需要自己输入需要订阅的星系代码，如J144038
2. 如果不考虑用户启用开发者模式通过接口数据获取星系&&解析所有home星系，重新评估前端后端实现的区别
2.1 你提到了分享链接的方式，是否可以理解为如果是后端实现，可以创建订阅（比如订阅了A星系和B星系，然后通过链接分享给其他人之后，这些人共享一个视图的变化和加载，而不是现在的home模式每个人本地有自己的一套视图互相不影响）
3. 告诉我这两种模式的复杂度区别和实现的区别
4. 展开说明一下这个问题，member可以设置home，viewer不可以设置
5. 如果自己的角色出现在了订阅星系的链接图以外，可以显示在他的订阅视图里。他只是看不到别人在订阅星系链接图外的路径。比如他订阅了A,B两个星系，他能看见和AB星系相连的所有星系链接视图（即使他的角色没有去过这些地方），然后他自己的角色没有经过这些星系，直接去了星系C D E，那么CDE也会出现在他的订阅视图里
6. lock星系只保留在全局视图里生效，在订阅模式下lock失去效果
7. 在新版本里，home星系只保留排序相关的功能，也就是可以对home星系右键选择rerange。不再具备其他特殊含义（订阅通过输入星系来订阅，而不是根据home筛选，订阅的星系也可以右键排序）所以理论上不需要迁移和其他引导，只要用户页面更新之后输入和确定自己需要订阅的星系即可渲染订阅的星系族
8. 修复这个注释

根据我的补充，重新思考和生成方案建议
